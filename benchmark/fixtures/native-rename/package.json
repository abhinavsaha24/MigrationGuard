{
  "name": "native-rename-fixture",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prestart:old": "npx prisma generate",
    "start:old": "ts-node src/index.ts",
    "prestart:new": "npx prisma generate",
    "start:new": "ts-node src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  }
}
