datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
  output   = "./generated/client"
}

model users {
  id        Int    @id @default(autoincrement())
  email     String
  full_name String
}
