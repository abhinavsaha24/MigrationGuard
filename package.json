{
  "name": "migrationguard-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*",
    "cli"
  ],
  "scripts": {
    "build": "npm run generate --workspace=@migrationguard/poc-app && npm run generate --workspace=@migrationguard/server && tsc -b && npm run build --workspace=@abhinavsaha24/migrationguard",
    "test": "vitest run",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "verify": "npm run verify --workspace=@abhinavsaha24/migrationguard",
    "benchmark": "npm run benchmark --workspace=@abhinavsaha24/migrationguard",
    "experiment:mutate": "node cli/dist/mutate.js",
    "ci:verify": "npm run build && npm run lint && npm run format:check && npm run test && (npm run verify || true) && npm run benchmark && npm run experiment:mutate"
  },
  "devDependencies": {
    "@eslint/js": "^9.10.0",
    "@types/node": "^26.2.0",
    "eslint": "^9.10.0",
    "prettier": "^3.3.3",
    "prettier-plugin-tailwindcss": "^0.8.1",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.5.0",
    "vitest": "^2.1.1"
  }
}
