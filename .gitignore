node_modules
dist
coverage
.env
.DS_Store
*.log

# TypeScript build state — never commit these; they contain absolute paths from
# the dev machine and cause tsc -b to skip rebuilds on a fresh clone.
*.tsbuildinfo

# Large binaries — download separately
bin/

# Generated reports (local verification runs)
reports/

# Test artifact binaries (upload boundary testing)
*.bin
downloaded.json

# Temporary benchmark working directories
benchmark/tmp*/

# CI/local script output
output.txt

# OS artifacts
Thumbs.db
**/.env

# Scratch directories
scratch/
**/scratch/

# Vite temp files
**/*.timestamp-*.mjs

# Root-level log artifacts
benchmark-baseline.log
mutate-baseline.log
run*.log
